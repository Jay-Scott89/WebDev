// string, number, bool, bigint, undefined, null, objest, sybol

// the keyword of var will save the variable in memory. Try and not use.
var score = 102;

// Use let as declare a variable
let scored = 103;
let name = "Jamie";
let isLoggedIn = true;

// Object
let teaTypes = ["lemon", "orange", "engligh"];
let user = { firstname: "Jamie", lastname: "Scott" };

let getScore = score;
console.log(getScore);
